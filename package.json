{
  "name": "marketplace-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "publish:clients": "git push origin HEAD:main && git push click HEAD:main && git push inova HEAD:main"
  },
  "engines": {
    "node": ">=18"
  }
}
